torch==2.3.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
ccxt==4.3.40
# ccxt only declares aiohttp>=3.8 (no upper bound), so an unpinned install
# resolves to whatever is newest — which, since ~aiohttp 3.10, rewrote its
# websocket reader/writer in C and dropped attributes (e.g. parse_frame)
# that ccxt.pro's websocket client reaches into directly. Pinned to the
# newest 3.9.x release (contemporaneous with ccxt==4.3.40) to keep that
# compatible; verified ccxt.pro's fast_client.py monkeypatch target
# (WebSocketReader.parse_frame) exists on this version.
aiohttp==3.9.5
cryptography==42.0.7
argon2-cffi==23.1.0
numpy==1.26.4
pydantic==2.7.1
pyjwt==2.8.0
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1
asyncpg==0.29.0
redis==5.0.4
modal==0.62.185
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
python-dotenv==1.0.1
# Test-only: SQLite async driver for backend/db in-memory tests (tests/test_db.py)
aiosqlite==0.20.0
