[pytest]
markers =
    integration: end-to-end integration tests that exercise multiple backend modules together (require the other units' code to be merged; skip gracefully via pytest.importorskip when a module is not yet present).
asyncio_mode = auto
testpaths = tests
